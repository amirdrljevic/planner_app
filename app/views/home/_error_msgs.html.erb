<% if model_name.errors.any? %>
  <div style="color: red">
    <h2><%= pluralize(model_name.errors.count, "error") %> prohibited it from being saved:</h2>

    <ul>
      <% model_name.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>